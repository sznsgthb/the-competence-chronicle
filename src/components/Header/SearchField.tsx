

function SearchField() {

    return (
        <input
            className="search-field"
            type="search"
            placeholder="Search..."
			// onChange={searchChange}
            />
  );
}

export default SearchField;